name: kwlib
version: 1.0.13
description: Widget Library for Kapp. Alls konar sameiginlegt fyrir Flutter Widgets
homepage: 
repository: github:marinonjalsson/kwlib.git
publish_to: none

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  very_good_analysis: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: '>=3.2.0 <4.0.0'

assets:
  - google_fonts/
