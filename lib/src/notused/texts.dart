/*
import 'package:flutter/material.dart';

class Texts extends StatelessWidget {
  Texts.paragraph(this.text, {Key? key})
      : textStyle = TextStyles.paragraph,
        super(key: key);
  Texts.title(this.text, {Key? key})
      : textStyle = TextStyles.title,
        super(key: key);
  Texts.subtitle(this.text, {Key? key})
      : textStyle = TextStyles.subtitle,
        super(key: key);
  Texts.message(this.text, {Key? key})
      : textStyle = TextStyles.message,
        super(key: key);
  Texts.error (this.text, {Key? key})
      : textStyle = TextStyles.error,
        super(key: key);
  Texts.textFieldLabel(this.text, {Key? key})
      : textStyle = TextStyles.textFieldLabel,
        super(key: key);
  final String text;
  final TextStyle textStyle;

  @override
  Widget build(BuildContext context) {
    return SelectableText(
      text,
      style: textStyle,
    );
  }
}
*/