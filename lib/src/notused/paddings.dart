/*
import 'package:flutter/material.dart' hide Padding;
import 'package:flutter/material.dart' as material show Padding;

class Paddings {
  static title({required Widget child}) => material.Padding(
        child: child,
        padding: const EdgeInsets.fromLTRB(0, 40, 0, 15),
      );
  static paragraph({required Widget child}) => material.Padding(
        child: child,
        padding: const EdgeInsets.fromLTRB(0, 10, 0, 10),
      );
  static buttton({required Widget child}) => material.Padding(
        child: child,
        padding: const EdgeInsets.fromLTRB(0, 10, 0, 10),
      );
}

*/
